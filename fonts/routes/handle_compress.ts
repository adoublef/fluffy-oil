import { Handler } from "~/deps.ts";

export function handleCompress(): Handler {
    return async (c)=> {
        // get file
        // run binary in bin directory
        // respond with output
    }
}