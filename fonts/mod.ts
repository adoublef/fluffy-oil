export { app as fonts } from "./service.ts";
// populate a map to with the font names and their location
// map will look like Record<'IBM Plex Mono', { pathname, weight, family }>
(async () => {
    // read content directory
})();
